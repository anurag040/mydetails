<app-header></app-header>
<div class="content-container">
  <router-outlet></router-outlet>
  <div *ngIf="isLoading" class="spinner-overlay">
    <app-spinner></app-spinner>
  </div>
</div>

.content-container {
  position: relative; /* Spinner overlay stays within this container */
  padding-top: 0; /* Prevents extra space between the header and content */
}

.spinner-overlay {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  display: flex;
  justify-content: center;
  align-items: center;
  background-color: rgba(255, 255, 255, 0.8); /* Semi-transparent overlay */
  z-index: 1000; /* Ensures spinner is above other content */
  pointer-events: none; /* Allows interaction with the header */
}
